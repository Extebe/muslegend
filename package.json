{
  "name": "mus-basque-v2",
  "version": "2.0.0",
  "description": "Jeu de Mus Basque en ligne",
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "dependencies": {}
}
